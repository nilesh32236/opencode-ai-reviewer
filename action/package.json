{
  "name": "@opencode-pr-agent/action",
  "version": "1.11.4",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o lib --minify && ncc build src/post-entry.ts -o lib/post --minify",
    "build:tsc": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/cache": "^4.0.1",
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.1",
    "@opencode-pr-agent/lib": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.44.1",
    "vitest": "^4.1.10"
  }
}
