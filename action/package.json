{
  "name": "@opencode-pr-agent/action",
  "version": "1.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o lib --minify && ncc build src/post.ts -o lib/post --minify",
    "build:tsc": "tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^1.1.3",
    "@actions/tool-cache": "^2.0.1",
    "@opencode-pr-agent/lib": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.3"
  }
}
