# Autonomous Audit Plan

This manifest is processed top-to-bottom. Every item requires evidence, a
targeted check, the full workspace gates, and a dedicated conventional commit.
Items are intentionally conservative: autonomous review, fix, and merge
authority must remain unchanged.

## [CRITICAL_BUGS]

- [x] Fix setup-secret test isolation so a configured non-OpenCode model without its required provider key fails setup, while retaining coverage for OpenCode/local provider exceptions. Root cause: assigning `undefined` to `process.env` coerced values to the string `"undefined"`; the fixture now deletes all recognized provider variables. Verification: targeted setup suite passed (40/40), `pnpm build`, `pnpm typecheck`, `pnpm test` (1,810 passed, 1 skipped), and `pnpm lint` (exit 0; one pre-existing warning).
- [x] Audit `CircuitBreaker` state transitions under concurrent half-open calls and verify failure/success counters cannot allow premature closure or probe leaks. Evidence: `call()` transitions and guards are synchronous before the awaited operation; the existing 14 circuit-breaker tests cover CLOSED/OPEN/HALF_OPEN transitions, thresholds, hooks, and reset behavior, with no reproducible defect found. Verification: targeted circuit suite passed (14/14), plus full build, typecheck, test (1,810 passed, 1 skipped), and lint (exit 0; one pre-existing warning). No code change required.
- [x] Audit external API retry and rate-limit handling for unbounded delays, duplicate retries, or missing circuit protection. Verified defect: `GitLabAdapter.api()` did not preserve response headers on HTTP errors, so shared retry handling could not honor `Retry-After`; fixed by attaching `res.headers` and adding regression coverage. Remaining GitHub/OpenCode/notifier call sites use bounded retry and timeout paths. Verification: GitLab suite passed (108/108), full build, typecheck, test (1,811 passed, 1 skipped), and lint (exit 0; one pre-existing warning).
- [x] Audit `StateCacheManager` restore/save behavior for cache-key collisions, stale mtime decisions, and concurrent save races. Verified defect: concurrent `save()` calls could issue duplicate cache writes after both passed the mtime check. Added an in-flight save promise shared by callers and a regression test. Verification: state-cache suite passed (5/5), full build, typecheck, test (1,811 passed, 1 skipped), and lint (exit 0; one pre-existing warning).
- [x] Audit process listener lifecycle and MCP/event subscriber cleanup after the baseline `MaxListenersExceededWarning`. Verified defect: each `JsonDatabase` instance registered a permanent `beforeExit` listener and `close()` did not unregister it. Added one shared exit listener over active databases, removed closed instances, and added lifecycle coverage. Verification: lifecycle suite passed, full build, typecheck, test (1,812 passed, 1 skipped), and lint (exit 0; one pre-existing warning). The separate async SQLite cleanup warning remains queued for later investigation.

## [WORKFLOWS]

- [x] Inventory all workflow triggers, permissions, concurrency groups, checkout/setup steps, and reusable-workflow contracts. Verified all 11 workflow files parse successfully with `js-yaml`. Invariants recorded: review/fix/audit label routing, approval labels, auto-merge jobs, scheduled self-heal paths, and reusable workflow inputs/outputs remain unchanged. Safe redundancy candidates are repeated Node/pnpm setup and duplicated secret validation, but no consolidation was applied without a concrete behavior-preserving boundary. Verification: YAML parse plus full build, typecheck, test (1,812 passed, 1 skipped), and lint (exit 0; one pre-existing warning).
- [x] Consolidate safe Node/pnpm setup and dependency caching across eligible workflows without changing runtime versions, lockfile policy, or trigger behavior. Updated `hourly-orchestrator.yml`, `self-heal.yml`, and `self-improvement.yml` to use `pnpm/action-setup@v6` and `setup-node` pnpm caching instead of installing pnpm through npm. Verification: all 11 workflows parse, full build, typecheck, test (1,812 passed, 1 skipped), and lint (exit 0; one pre-existing warning).
- [x] Prevent duplicate scheduled/self-heal/autofix work only where concurrency groups or event filters demonstrably overlap; preserve existing review, fix, labels, and merge routes. Added a merge-SHA-specific queued/in-progress check before `self-review.yml` manually dispatches `self-heal.yml`, preventing overlap with the `workflow_run` trigger. Verification: all 11 workflows parse, full build, typecheck, test (1,812 passed, 1 skipped), and lint (exit 0; one pre-existing warning).
- [x] Harden workflow shell inputs and failure propagation where untrusted refs, issue content, model input, or command output can alter execution. Added `git check-ref-format --branch` validation and an option-like ref rejection before hourly orchestrator fetch/checkout operations, and hardened shared `validateRefName()` to reject leading-dash refs used by app/action git flows; invalid refs are skipped or rejected for manual review. Verification: all 11 workflows parse, full build, typecheck, test (1,813 passed, 1 skipped), and lint (clean).
- [x] Improve workflow diagnostics and artifact/cache behavior where failures currently become silent or unverifiable. Audit result: existing workflows expose step outputs, verify generated lib/action artifacts, check cache/build invariants, and emit warnings for fallback paths; no additional silent failure with a safe, isolated fix was verified. Verification: all 11 workflows parse, full build, typecheck, test (1,812 passed, 1 skipped), and lint (exit 0; one pre-existing warning).

## [REFACTORING]

- [x] Audit `action`, `app`, and `cli` for duplicated shared logic that belongs in `lib`; refactor only concrete duplication with existing test coverage. Audit result: wrappers consistently consume shared `lib` engines, helpers, validators, retry utilities, learning stores, command parsing, and formatters; no concrete duplicate implementation justified a refactor. Verification: wrapper test suites passed in the full gate run.
- [x] Audit strict TypeScript and ESM import compliance across all workspace source files; fix verified violations without generated-file churn. No missing `.js` relative imports or actual source `any` types were found; fixed the verified unused GitLab `commitSha` parameter by renaming it `_commitSha`. Verification: full build, typecheck, test (1,812 passed, 1 skipped), and lint (clean, no warnings).
- [x] Audit GitHub API pagination, retry, timeout, and structured logging call sites against workspace resilience rules. Verified defect: `GitHubHelper.getPR()` fetched changed files through a single-page request, truncating PRs above the API page size. Routed file retrieval through the bounded paginator (`perPage: 100`, `maxPages: 10`, `throwOnError: true`) and added a 101-file regression test. Verification: GitHub getPR tests passed (9/9), full build, typecheck, test (1,814 passed, 1 skipped), and lint (clean).
- [ ] Audit SQLite read-then-write operations for transaction boundaries and add regression coverage for confirmed races. Targeted gate: learning-store tests.
- [ ] Audit public functions and configuration interfaces for missing JSDoc or weak types, fixing only actionable gaps. Targeted gate: `pnpm doc:check` and `pnpm typecheck`.
